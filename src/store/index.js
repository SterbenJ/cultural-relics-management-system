import Vue from 'vue'
import Vuex from 'vuex'

// state
import stateMain from './state/stateMain.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: stateMain,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
